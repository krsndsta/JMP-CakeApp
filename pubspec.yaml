name: jmp_3
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  d_info: ^0.2.2
  d_input: ^0.1.3
  d_view: ^0.0.3
  flutter:
    sdk: flutter
  geocoding: ^3.0.0
  geolocator: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
